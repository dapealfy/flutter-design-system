import 'package:flutter/material.dart';

class FunDsTextController extends TextEditingController {
  FunDsTextController({String? text}) : super(text: text);

  // Empty for now, prepare for future use
}
