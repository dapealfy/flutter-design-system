import 'package:flutter/material.dart';

class HomeCatalog extends StatelessWidget {
  const HomeCatalog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Welcome to the Design System Catalog!',
    );
  }
}
